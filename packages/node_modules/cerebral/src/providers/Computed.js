import { ensurePath } from '../utils'
import Provider from '../Provider'

export default Provider({
  get(path, props = {}) {
    path = ensurePath(path)
    const computed = this.context.controller.getComputed(path)

    return computed.getValue(props)
  },
})
