import Provider from '../Provider'
import { Compute } from '../Compute'

export default Provider(
  (context) => (tag) => {
    const value = context.resolve.value(tag)
    if (value instanceof Compute) {
      return value.getValue(context.props)
    }

    return value
  },
  {
    wrap: false,
  }
)
