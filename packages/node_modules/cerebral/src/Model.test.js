/* eslint-env mocha */
import { Module } from './'
import BaseController from './BaseController'
import Model from './Model'
import assert from 'assert'

describe('Model', () => {
  test('should instantiate with initial state', () => {
    const rootModule = Module({
      state: {
        foo: 'bar',
      },
    })
    const model = new BaseController(rootModule, {
      Model,
    }).model
    assert.equal(model.get(['foo']), 'bar')
  })
  test('should grab nested state', () => {
    const rootModule = Module({
      state: {
        foo: {
          bar: 'value',
        },
      },
    })
    const model = new BaseController(rootModule, {
      Model,
    }).model
    assert.equal(model.get(['foo', 'bar']), 'value')
  })
  test('should be able to flush changed paths', () => {
    const rootModule = Module({
      state: {
        foo: {
          bar: 'value',
        },
      },
    })
    const model = new BaseController(rootModule, {
      Model,
    }).model
    model.set(['foo', 'bar'], 'value2')
    assert.deepEqual(model.flush(), [
      {
        path: ['foo', 'bar'],
        forceChildPathUpdates: true,
      },
    ])
  })
  test('should flush same path changes correctly', () => {
    const rootModule = Module({
      state: {
        foo: {
          bar: 'value',
        },
      },
    })
    const model = new BaseController(rootModule, {
      Model,
    }).model
    model.set(['foo', 'bar'], 'value2')
    model.set(['foo', 'bar'], 'value3')
    assert.deepEqual(model.flush(), [
      {
        path: ['foo', 'bar'],
        forceChildPathUpdates: true,
      },
      {
        path: ['foo', 'bar'],
        forceChildPathUpdates: true,
      },
    ])
  })
  test('should throw when updating invalid path', () => {
    const rootModule = Module({
      state: {
        foo: 'bar',
      },
    })
    const model = new BaseController(rootModule, {
      Model,
    }).model
    assert.throws(() => {
      model.set(['foo', 'bar'], 'baz2')
    })
  })
  test('should emit mutation events', () => {
    let hasEmitted = false
    const rootModule = Module({
      state: {
        foo: 'bar',
      },
    })
    const controller = new BaseController(rootModule, {
      Model,
    })
    controller.on('mutation', () => {
      hasEmitted = true
    })
    controller.model.set(['foo'], 'baz2')
    assert.ok(hasEmitted)
  })
  test('should throw when updating invalid nested path', () => {
    const rootModule = Module({
      state: {
        foo: 'bar',
      },
    })
    const model = new BaseController(rootModule, {
      Model,
    }).model
    assert.throws(() => {
      model.set(['foo', 'bar', 'baz'], 'baz2')
    })
  })

  describe('SET', () => {
    test('should be able to set state', () => {
      const rootModule = Module({
        state: {},
      })
      const model = new BaseController(rootModule, {
        Model,
      }).model

      model.set(['foo'], 'bar')
      assert.deepEqual(model.get(), { foo: 'bar' })
    })
  })
  describe('TOGGLE', () => {
    test('should be able to toggle value', () => {
      const rootModule = Module({
        state: {
          foo: true,
        },
      })
      const model = new BaseController(rootModule, {
        Model,
      }).model
      model.toggle(['foo'])
      assert.deepEqual(model.get(), { foo: false })
    })
  })
  describe('PUSH', () => {
    test('should be able to push to array', () => {
      const rootModule = Module({
        state: {
          list: [],
        },
      })
      const model = new BaseController(rootModule, {
        Model,
      }).model
      model.push(['list'], 'bar')
      assert.deepEqual(model.get(), { list: ['bar'] })
    })
  })
  describe('MERGE', () => {
    test('should be able to merge objects', () => {
      const rootModule = Module({
        state: {
          foo: {
            valA: 'foo',
          },
        },
      })
      const model = new BaseController(rootModule, {
        Model,
      }).model
      model.merge(['foo'], { valB: 'bar' })
      assert.deepEqual(model.get(), { foo: { valA: 'foo', valB: 'bar' } })
    })
    test('should flush changes to merged keys when object exists', () => {
      const rootModule = Module({
        state: {
          foo: {
            valA: 'foo',
          },
        },
      })
      const model = new BaseController(rootModule, {
        Model,
      }).model
      model.merge(['foo'], { valB: 'bar' })
      assert.deepEqual(model.flush(), [
        {
          path: ['foo', 'valB'],
          forceChildPathUpdates: true,
        },
      ])
    })
    test('should flush change on object only if no existing object', () => {
      const rootModule = Module({
        state: {
          foo: null,
        },
      })
      const model = new BaseController(rootModule, {
        Model,
      }).model
      model.merge(['foo'], { valB: 'bar' })
      assert.deepEqual(model.flush(), [
        {
          path: ['foo'],
          forceChildPathUpdates: true,
        },
      ])
    })
  })
  describe('POP', () => {
    test('should be able to pop arrays', () => {
      const rootModule = Module({
        state: {
          list: ['foo', 'bar'],
        },
      })
      const model = new BaseController(rootModule, {
        Model,
      }).model
      model.pop(['list'])
      assert.deepEqual(model.get(), { list: ['foo'] })
    })
  })
  describe('SHIFT', () => {
    test('should be able to shift arrays', () => {
      const rootModule = Module({
        state: {
          list: ['foo', 'bar'],
        },
      })
      const model = new BaseController(rootModule, {
        Model,
      }).model
      model.shift(['list'])
      assert.deepEqual(model.get(), { list: ['bar'] })
    })
  })
  describe('UNSHIFT', () => {
    test('should be able to unshift arrays', () => {
      const rootModule = Module({
        state: {
          list: ['foo'],
        },
      })
      const model = new BaseController(rootModule, {
        Model,
      }).model
      model.unshift(['list'], 'bar')
      assert.deepEqual(model.get(), { list: ['bar', 'foo'] })
    })
  })
  describe('SPLICE', () => {
    test('should be able to splice arrays', () => {
      const rootModule = Module({
        state: {
          list: ['foo', 'bar'],
        },
      })
      const model = new BaseController(rootModule, {
        Model,
      }).model
      model.splice(['list'], 1, 1, 'bar2')
      assert.deepEqual(model.get(), { list: ['foo', 'bar2'] })
    })
  })
  describe('UNSET', () => {
    test('should be able to unset keys', () => {
      const rootModule = Module({
        state: {
          foo: 'bar',
        },
      })
      const model = new BaseController(rootModule, {
        Model,
      }).model
      model.unset(['foo'])
      assert.deepEqual(model.get(), {})
    })
    test('should flush unset paths', () => {
      const rootModule = Module({
        state: {
          foo: {
            bar: 'value',
          },
        },
      })
      const model = new BaseController(rootModule, {
        Model,
      }).model
      model.unset(['foo', 'bar'])
      assert.deepEqual(model.flush(), [
        {
          path: ['foo', 'bar'],
          forceChildPathUpdates: true,
        },
      ])
    })
  })
  describe('CONCAT', () => {
    test('should be able to concat array', () => {
      const rootModule = Module({
        state: {
          foo: ['foo'],
        },
      })
      const model = new BaseController(rootModule, {
        Model,
      }).model
      model.concat(['foo'], ['bar'])
      assert.deepEqual(model.get(), { foo: ['foo', 'bar'] })
    })
  })
  describe('INCREMENT', () => {
    test('should increment with default of 1', () => {
      const rootModule = Module({
        state: {
          foo: 3,
        },
      })
      const model = new BaseController(rootModule, {
        Model,
      }).model
      model.increment(['foo'])
      assert.deepEqual(model.get(), { foo: 4 })
    })
    test('should increment with given value', () => {
      const rootModule = Module({
        state: {
          foo: 3,
        },
      })
      const model = new BaseController(rootModule, {
        Model,
      }).model
      model.increment(['foo'], 2)
      assert.deepEqual(model.get(), { foo: 5 })
    })
    test('should raise on invalid target', () => {
      const rootModule = Module({
        state: {
          foo: 'hello',
        },
      })
      const model = new BaseController(rootModule, {
        Model,
      }).model
      assert.throws(() => model.increment(['foo'], 2), 'xxx')
    })
    test('should raise on invalid increment value', () => {
      const rootModule = Module({
        state: {
          foo: 4,
        },
      })
      const model = new BaseController(rootModule, {
        Model,
      }).model
      assert.throws(() => model.increment(['foo'], 'hop'), 'xxx')
    })
  })
  describe('Serializable', () => {
    test('should make value forceSerializable when devtools are attached', () => {
      const rootModule = Module({
        state: {
          foo: 'bar',
        },
      })
      const model = new BaseController(rootModule, {
        Model,
        devtools: {
          allowedTypes: [Date],
          init() {},
        },
      }).model
      model.set(['foo'], new Date())
      assert.equal(model.state.foo.toJSON(), '[Date]')
    })
    test('should throw error if value inserted is not serializable', () => {
      const rootModule = Module({
        state: {
          foo: 'bar',
        },
      })
      const model = new BaseController(rootModule, {
        Model,
        devtools: { init() {} },
      }).model
      assert.throws(() => {
        model.set(['foo'], new Date())
      })
    })
    test('should NOT throw error if value inserted is serializable', () => {
      const rootModule = Module({
        state: {
          foo: 'bar',
        },
      })
      const model = new BaseController(rootModule, {
        Model,
        devtools: { init() {} },
      }).model
      assert.doesNotThrow(() => {
        model.set(['foo'], [])
      })
    })
    test('should NOT throw error if passing allowed type in devtools', () => {
      const rootModule = Module({
        state: {
          foo: 'bar',
        },
      })
      const model = new BaseController(rootModule, {
        Model,
        devtools: { allowedTypes: [Date], init() {} },
      }).model
      assert.doesNotThrow(() => {
        model.set(['foo'], new Date())
      })
    })
  })
})
