/* eslint-env mocha */

const { compileDirectory } = require('./helper')

describe('TypeScript definitions for cerebral', function() {
  this.timeout(15000)
  it('should compile against index.d.ts', compileDirectory)
})
