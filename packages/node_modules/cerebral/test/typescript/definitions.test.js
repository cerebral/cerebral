/* eslint-env mocha */
import { compileDirectory } from './helper'

describe('TypeScript definitions for cerebral', () => {
  this.timeout(15000)
  test('should compile against index.d.ts', compileDirectory)
})
