import { Compute, ValueResolver } from '../../index';
import { compute } from '../../index';
import { state } from './../../tags';

export const computeName: Compute = compute(
    state`app.ready`,
    (ready: boolean, get: ValueResolver) => ({
        name: get(state`app.name`) + (ready ? ' is ready' : '')
    })
)