import * as React from 'react'
import { decorator as connect } from '../../react'
import { state } from '../../tags'
import { computeName } from './computed';

@connect({
  composing: state`composer.$running`,
  error: state`composer.$error`,
  name: computeName 
})
export class Running extends React.Component {
  render() {
    return React.createElement('div', {}, 'hello')
  }
}