import { Compute, ValueResolver } from 'cerebral'
import { state } from 'cerebral/tags'

export const someComputed: { name: string } = Compute((get: ValueResolver) => ({
  name: get(state`app.name`) + (get(state`app.ready`) ? ' is ready' : ''),
}))
