import { Computed, ValueResolver } from 'cerebral'
import { state } from 'cerebral/tags'

export const someComputed: { name: string } = Computed({
  ready: state`app.ready`,
})(({ ready, get }: { ready: boolean; get: ValueResolver }) => ({
  name: get(state`app.name`) + (ready ? ' is ready' : ''),
}))
