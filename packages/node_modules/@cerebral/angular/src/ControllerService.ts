import { Controller, ModuleClass, ControllerOptions } from 'cerebral'

export class ControllerService {
  constructor(rootModule: ModuleClass, options?: ControllerOptions) {
    return Controller(rootModule, options)
  }
}
