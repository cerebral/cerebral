import { App, ModuleClass, ControllerOptions } from 'cerebral'

export class AppService {
  constructor(rootModule: ModuleClass, options?: ControllerOptions) {
    return App(rootModule, options)
  }
}
