import { Component, ChangeDetectionStrategy, Input } from '@angular/core'

@Component({
    inputs: ['subCompObj'] ,
    selector: 'sub-component',
    template: `        
        <div *ngIf="subCompObj">
            {{subCompObj.name}}
          <h2>{{subCompObj.name}} details!</h2>
          <div><label>id: </label>{{subCompObj.id}}</div>
          <div><label>freq: </label>{{subCompObj.freq}}</div>
        </div>
    `,
    changeDetection: ChangeDetectionStrategy.OnPush,
})
export class TestSubComponent {
    @Input() subCompObj: any;
    name: string = 'TestSubComponent';
    constructor() {
    }
}
