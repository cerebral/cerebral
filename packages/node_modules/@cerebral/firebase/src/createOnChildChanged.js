import { listenTo, createRef } from './helpers'

export default function createOnChildChanged(context) {
  return (path, signal, options = {}) => {
    listenTo(createRef(path, options), path, 'child_changed', signal, data => {
      context.controller.getSignal(signal)(
        Object.assign(
          {
            key: data.key,
            value: data.val(),
          },
          options.payload || {}
        )
      )
    })
  }
}
