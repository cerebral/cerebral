import * as firebase from '../../operators'
import { state, props, string } from 'cerebral/tags'

const signal = [ 
  firebase.value ( props`some.path` ),
  firebase.set ( string`remote.${ props`path` }`, state`foo.bar` ),
]