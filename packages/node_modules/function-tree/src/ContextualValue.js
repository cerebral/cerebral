export default class ContextualValue {
  // "getValue" should receive a context to extract the value
  getValue() {
    throw new Error(
      'Extending ContextualValue requires you to add a "getValue" method'
    )
  }
}
