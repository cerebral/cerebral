import detectFeatures from '../factories/detectFeatures'
import setScreenSpecs from '../actions/setScreenSpecs'
import setUserAgent from '../factories/setUserAgent'

const moduleStarted = [
  detectFeatures,
  setScreenSpecs,
  setUserAgent
]

export default moduleStarted
